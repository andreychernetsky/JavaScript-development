<html>
<body>
  <script>
   'use stric'

/*задача: 1) Создать возможность для вводя числа пользователем.
  2) сделать проверку на пустую строку, если число не введено и если введено не число
3)если число, то помещаем его в переменную для суммирования с каждым новым веденным числом.
  */
const numbers = [];

while (true) {
  let value = prompt('введите число', 0);
// проверям, что значение не равно строке, пустому значению
  if (value === '' || value === null || isNaN(value) ) break;

  numbers.push(+value);//добавляем числов массив
}

let sum = 0;

for (let i = 0; i < numbers.length; i++) {
  sum += numbers[i];//помещаем число в пременную sum для суммирования
}

alert( sum );
  </script>

  <!-- творческое задание:
   1) проработать данный пример через функцию. (написать свой вариант, как понимаешь)
  2) написать ввод значения не через prompt а input 
  
  3) Добавить таймер, чтобы через 3 сек по клику на кнопку сработал результат
  4) дописать условия проверки. 
    - если введено не число. выдать сообщение 'введите одно число'
    - если введено не число, а буква. 'введите только цифру'
    - если не ввел ни одного значения 'Введите одну цифру' 
    - все цифры должны быть только в нижнем регистре
  -->

</body
</html>
