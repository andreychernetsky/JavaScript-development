<html>

<body>
  <script>
    'use strict';
    function compareNumeric(a, b) {
      return b - a;
    }

    const arr = [5, 2, 1, -10, 8];

    // console.log( arr.sort(compareNumeric) );  // 1, 2, 15

    //////////////////////////////////////////////////////////////////////

    const arr1 = ["HTML", "JavaScript", "CSS"];

    const arr1Sorted = arr1.slice().sort();

    // console.log( arr1Sorted ); // CSS, HTML, JavaScript
    // console.log( arr1 ); // HTML, JavaScript, CSS (без изменений)

    //////////////////////////////////////////////////////////////////////
    function random() {

    return Math.random() - 0.5;

    }

    const arr = [1, 2, 3, 4, 5];

    arr.sort(random);

    const( arr ); // элементы в случайном порядке, например [3,5,1,2,4]

    //////////////////////////////////////////////////////////////////////////

  </script>
</body>

</html>
