<html>
<body>
<script>
  'use strict'

  const neo = new Array(10);

  for (let i = 0; i < neo.length; i++) {
    neo[i] = new Array(10);
  }

  for (let row = 0; row < neo.length; row++) {
    for (let col = 0; col < neo.length; col++) {
      neo[row][col] = row * col;
    }
  }

  alert(neo[6][9]);
  alert(neo[2][5]);
  alert(neo[3][3]);

</script>
</body>
</html>
